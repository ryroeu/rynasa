# Copy this file to .env and add your key from https://api.nasa.gov
# If unset (or python-dotenv is not installed), the server falls back to the
# heavily rate-limited DEMO_KEY. The real .env is gitignored — never commit it.
NASA_API_KEY=
